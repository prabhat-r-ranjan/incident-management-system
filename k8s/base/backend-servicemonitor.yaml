apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: incident-backend-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: incident-backend

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s